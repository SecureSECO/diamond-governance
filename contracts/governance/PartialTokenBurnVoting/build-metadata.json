{
    "ui": "",
    "change": "",
    "pluginSetupABI": {
      "prepareInstallation": [
        "tuple(tuple(bool earlyExecution, bool burnTokens, uint8 partialVotingSettings) votingMode, uint64 supportThreshold, uint64 minParticipation, uint64 minDuration, uint256 minProposerVotingPower) votingSettings",
        "tuple(string name, string symbol) tokenSettings",
        "tuple(address[] receivers, uint256[] amounts) mintSettings"
      ],
      "prepareUpdate": [""],
      "prepareUninstallation": [""]
    }
  }
  